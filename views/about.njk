{% extends "layout.njk" %}
{% block content%}
{%block head%}
    <title>sobre -Rafaela Alencar</title>
{%endblock%}

    <header>
        <div class="links">
            <a href="/portfolio">Aulas</a>
        </div>
        
    </header>


    <div id="wrapper">
        <img src="{{about.avatar_url}}" alt="imagem de {{about.name}}">
        <h1>{{about.name}}</h1>
        <h2>{{about.role}}</h2>
        <p> {{about.description}}</p>
    </div>

    <div id="links-footer" class="links">
        {% for link in about.links %}
        <a href="{{ link.url}}" target="_blank">{{link.name}}</a>
        
        {% endfor %}
    </div>

{% endblock %}


